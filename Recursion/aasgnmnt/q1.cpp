#include<bits/stdc++.h>
using namespace std;
void revArr(int i,int arr[],int n){
    if(i>=n) return;
    return (arr[i+1],)
}
int main(){
    int arr[10];
    for(auto it: arr)
    cin>>arr[it];
    int i=0;
    revArr(i,arr,10);
}