#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    getline(cin,s);
    int n=s.length();
    string ss=s.substr(n/2);
    cout<<ss<< endl ;
    // reversing--->
    // reverse(ss.begin(),ss.end());
    // cout<<ss;
}